<p id="notice"><%= notice %></p>

<h1>Listing Boats</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Capacity</th>
      <th>Size</th>
      <th>Price per day</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @boats.each do |boat| %>
      <tr>
        <td><%= boat.name %></td>
        <td><%= boat.capacity %></td>
        <td><%= boat.size %></td>
        <td><%= boat.price_per_day %></td>
        <td><%= boat.description %></td>
        <td><%= link_to 'Show', harbour_boat_path(boat.harbour, boat) %></td>
        <td><%= link_to 'Edit', edit_harbour_boat_path(boat.harbour, boat) %></td>
        <td><%= link_to 'Destroy', harbour_boat_path(boat.harbour, boat), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Boat', new_harbour_boat_path %>



